{
    "tasks": [
        {
            "type": "shell",
            "label": "build filesystem",
            "command": "make",
            "options": {
                "cwd": "${workspaceFolder}/src"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ],
    "version": "2.0.0"
}